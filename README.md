# credit-risk-logistic-regression

This project predicts whether a customer will default on their credit card using Logistic Regression. It compares two Python libraries, Scikit-learn and Statsmodels, on a dataset of 45,528 customers.

## Libraries
- pandas, numpy
- matplotlib, seaborn
- scikit-learn
- statsmodels

## How to Run
1. Install libraries: `pip install pandas numpy matplotlib seaborn scikit-learn statsmodels`
2. Open `credit_risk.ipynb` in Jupyter Notebook
3. Run cells from top to bottom

## Results
Both models achieved 98% accuracy with an AUC score of 0.993+.

## Module
CO7024 Statistical Programming — University of Chester
